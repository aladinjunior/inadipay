<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.aladinjunior.inadipay">

    <application
        android:name=".util.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InadiPay">
        <activity android:name=".register.view.RegisterNextActivity"></activity>
        <activity
            android:name=".register.view.RegisterActivity"
            android:exported="true"></activity>
        <activity
            android:name=".detailed.view.DetailedCostumerInfoActivity"
            android:exported="true"></activity>
        <activity
            android:name=".main.view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>